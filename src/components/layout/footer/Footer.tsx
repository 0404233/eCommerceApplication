import { ReactElement } from 'react';
import styles from './footer.module.css';

export default function Footer(): ReactElement {
  return <footer className={styles['footer']}>E-Commerce Application</footer>;
}
